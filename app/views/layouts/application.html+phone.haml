!!!
%html
%head
  %title GameBuddy - Search Girls and Boys around you who play your favourite game & play with them.
  = stylesheet_link_tag    'application', media: 'all'
  = javascript_include_tag 'application'
  %link{:rel => "stylesheet", :href => "https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css"}
  = csrf_meta_tags
%body
  %header
    .wrapper.clearfix
      #logo= link_to "GameBuddy", root_path
      %nav
        - if user_signed_in?
          .dropdown2
            %button.dropbtn See Profile
            .dropdown2-content
              = link_to "Go to Your Profile", current_user.profile
              = link_to "Go to Messages", user_path(current_user)
          = link_to "Log out", destroy_user_session_path, :method => :delete
        - else
          = link_to "SIGN IN OR SIGN UP", new_user_registration_path

  %p.notice= notice
  %p.alert= alert
  .wrapper-m
    = yield
