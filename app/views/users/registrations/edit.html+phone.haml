.form-style-profile-m
  %p#instavoto2-m EDIT YOUR E-MAIL & PASSWORD:
  = simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f|
    %h1= f.error_notification
    .form-inputs
      %h1 You have to confirm your current Password to Edit.
      %h1= f.input :email,  label: "E-Mail:", required: true, autofocus: true, :input_html => {class: 'login-m'}

      - if devise_mapping.confirmable? && resource.pending_reconfirmation?
        %p
          Currently waiting confirmation for: #{resource.unconfirmed_email}
      %h1= f.input :password, label: "Password:(Leave it blank if you don't want to change it.)", autocomplete: "off", required: false, placeholder: "Leave it blank if you don't want to change your current Password", :input_html => {class: 'login-m'}
      %h1= f.input :password_confirmation, label: "Password Confirmation:", required: false, placeholder: "Confirm your new Password", :input_html => {class: 'login-m'}
      %h1= f.input :current_password, label: "Current Password:(We need your current password to confirm your changes.)", required: true, placeholder: "Type your current Password to confirm Changes.", :input_html => {class: 'login-m'}
    .form-actions
      = submit_tag "Update Now", name: nil, class: "button-m2"

  %p#official-m2 Wants to Delete your Account? Delete from Below:
  %p#official-m2= link_to "Delete my Account Now.", registration_path(resource_name), data: { confirm: "Are you sure to Delete your Account. It will erase Everything(Your Received and Sent Posts.)?" }, method: :delete
